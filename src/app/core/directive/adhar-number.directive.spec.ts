import { AdharNumberDirective } from './adhar-number.directive';

describe('AdharNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new AdharNumberDirective();
    expect(directive).toBeTruthy();
  });
});
